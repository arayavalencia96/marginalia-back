services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_NAME:-marginalia}
      POSTGRES_USER: ${DB_USER:-marginalia}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-marginalia}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  postgres_data:
